describe("Module Name", () => {
	describe("Function Name", () => {
		beforeEach(() => {
		});

		afterEach(() => {
		});

		it("Test", async () => {
			return true;
			// setup
			// exercise
			// verify
		});
	});
});
